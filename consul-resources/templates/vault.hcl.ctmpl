backend "consul" {
  address = "{{ env "CONSUL_HOSTNAME" }}:8500"
  path = "vault"
  advertise_addr = "http://{{ env "LOCAL_IP4_ADDRESS" }}:8200"
}

listener "tcp" {
 address = "{{ env "LOCAL_IP4_ADDRESS" }}:8200"
 tls_disable = 1
}
